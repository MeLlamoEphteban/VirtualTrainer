@model IEnumerable<VirtualTrainer.Subscription>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SubName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AllowedTimeInterval)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SubName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AllowedTimeInterval)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idsubscription">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idsubscription">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idsubscription">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
